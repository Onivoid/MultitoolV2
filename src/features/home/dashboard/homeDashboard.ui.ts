/** Layout racine des contenus widget — remplit la hauteur `heightPx` du shell. */
export const HOME_WIDGET_ROOT = "flex h-full min-h-0 flex-col";

/** Zone liste scrollable — s'étire avec le resize vertical. */
export const HOME_WIDGET_SCROLL = "min-h-0 flex-1 overflow-y-auto";

/** Barre d'actions / footer fixe en bas du widget. */
export const HOME_WIDGET_FOOTER = "shrink-0";
